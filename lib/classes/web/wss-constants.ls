const EVENT_READY = \ready
const EVENT_CONFIGURED = \configured
const EVENT_DATA = \data
const REQ_CONFIGURE = \configure

module.exports = exports = {EVENT_READY, EVENT_CONFIGURED, EVENT_DATA, REQ_CONFIGURE}
